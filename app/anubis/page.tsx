import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Anubis',
  description: 'Обозначения позиций и раскидки гранат на карте Anubis'
};

export default function Anubis() {
  return <main>Anubis</main>;
}
